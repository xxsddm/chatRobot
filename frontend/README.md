# Chat Service Frontend

基于 Vue 3 + TypeScript + Element Plus 构建的现代化聊天应用前端。

## 功能特性

- 💬 实时聊天界面
- 🔄 流式响应支持
- 📱 响应式设计
- 🎨 现代化UI界面
- 💾 会话管理
- 🚀 快速开发环境

## 技术栈

- **Vue 3** - 渐进式JavaScript框架
- **TypeScript** - 类型安全的JavaScript
- **Element Plus** - Vue 3组件库
- **Pinia** - 状态管理
- **Vite** - 构建工具
- **Vue Router** - 路由管理

## 快速开始

### 安装依赖

```bash
npm install
```

### 开发环境

```bash
npm run dev
```

应用将在 http://localhost:3000 启动

### 构建生产版本

```bash
npm run build
```

### 代码检查

```bash
npm run lint
```

### 格式化代码

```bash
npm run format
```

## 项目结构

```
src/
├── components/          # Vue组件
│   └── ChatInterface.vue # 主要聊天界面组件
├── stores/               # Pinia状态管理
│   └── chat.ts          # 聊天相关状态
├── views/               # 页面视图
│   └── ChatView.vue     # 聊天页面
├── router/              # 路由配置
├── App.vue              # 根组件
└── main.ts              # 应用入口
```

## API 配置

前端通过代理配置访问后端API，在 `vite.config.ts` 中配置：

```typescript
server: {
  port: 3000,
  proxy: {
    '/api': {
      target: 'http://localhost:8080',  // 后端服务地址
      changeOrigin: true,
      secure: false
    }
  }
}
```

## 功能说明

### 聊天功能
- 支持发送文本消息
- 流式显示AI回复
- 显示消息时间戳
- 自动滚动到最新消息

### 会话管理
- 创建新会话
- 切换历史会话
- 删除会话
- 清除当前会话历史

### 界面特性
- 现代化的聊天界面
- 响应式设计，支持移动端
- 优雅的加载状态
- 友好的错误提示

## 开发建议

1. **组件化开发** - 保持组件的独立性和可复用性
2. **类型安全** - 充分利用TypeScript的类型检查
3. **状态管理** - 使用Pinia管理应用状态
4. **代码规范** - 遵循ESLint和Prettier的配置

## 部署

构建完成后，可以将 `dist` 目录中的文件部署到任何静态文件服务器上。

如果是部署到Spring Boot项目中，可以将构建后的文件复制到 `src/main/resources/static` 目录下。

## 推荐的IDE设置

[VSCode](https://code.visualstudio.com/) + [Volar](https://marketplace.visualstudio.com/items?itemName=Vue.volar) (并禁用Vetur).

## TypeScript对`.vue`导入的类型支持

TypeScript默认无法处理`.vue`导入的类型信息，所以我们用`vue-tsc`替换`tsc`进行类型检查。在编辑器中，我们需要[Volar](https://marketplace.visualstudio.com/items?itemName=Vue.volar)来让TypeScript语言服务识别`.vue`类型。

## 自定义配置

参见 [Vite配置参考](https://vitejs.dev/config/)。
